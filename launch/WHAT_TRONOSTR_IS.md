TRONostr is an open realtime TRON event layer.

It converts TRON chain activity into structured machine-readable telemetry, distributes those events over Nostr as the first/default transport, and can optionally mirror the same event contract to webhook consumers.
